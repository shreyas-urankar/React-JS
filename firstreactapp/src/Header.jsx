function Header(headerInfo, cname)
{
    // let {headerInfo, cname}=props;
    // console.log(headerInfo, cname)
    return (
        <div>
            <div>Welcome to Header Section {headerInfo} | {cname}</div>
            <div>Hello Uranksr Family.</div>
            <div>Hi who are you?</div>
        </div>
    )
}

export default Header;